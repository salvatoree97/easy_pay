/// Extends this class to create custom host from the project using the ENVIRONMENT
class Host {
  final String value;

  const Host(this.value);

  static const Host example = Host('https://test');
}
